









/*const array = [];

let somma = 0;

while ( somma <= 50){
     const userNumber = parseInt(prompt('type a new number'));
    if ( !isNaN (userNumber)){
     somma += userNumber;
     array.push(userNumber);
}
}
